Question : first word will be in small latters then camelcasing will come live saveOurBoys so if lower case than convert it to upper case and vise versa and need the perticular words in every rows 
input : saveOurBoys
output: SAVE
        oUR
        bOYS
#include <iostream>
using namespace std;
int main() {
   string s;
   cin>>s;
   int n=s.size();
   for(int i=0;i<n;i++){
       if(islower(s[i])){
           char ch=toupper(s[i]);
           cout<<ch;
       }
       else if(isupper(s[i])){
           cout<<endl;
           char ch=tolower(s[i]);
           cout<<ch;
       }
   }
}
